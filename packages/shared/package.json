{
    "name": "@imagina-crm/shared",
    "version": "0.1.0",
    "private": true,
    "description": "Zod schemas y tipos compartidos front↔back. Un shape, una definición, cero drift.",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "files": ["dist"],
    "scripts": {
        "build": "tsc -p tsconfig.build.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "lint": "eslint src",
        "test": "vitest run"
    },
    "dependencies": {
        "zod": "^3.25.76"
    },
    "devDependencies": {
        "@eslint/js": "^9.30.0",
        "eslint": "^9.30.0",
        "typescript": "^5.7.3",
        "typescript-eslint": "^8.35.0",
        "vitest": "^3.2.4"
    }
}
